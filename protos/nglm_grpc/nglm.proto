syntax = "proto3";

package nglm_grpc;

service Server {
	rpc register(clientInfo) returns (response) {}
}

message clientInfo {
	string hostname = 1;
	string ipv4 = 2;
	int32 port = 3;
}

message response {
	bool success = 1;
}